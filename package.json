{
  "name": "gerome",
  "version": "1.0.0",
  "description": "custom error",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "gerome",
  "license": "ISC",
  "dependencies": {
    "requirejs": "^2.3.2",
    "util": "^0.10.3"
  }
}
